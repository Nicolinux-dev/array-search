# Array Search

Este projeto busca um valor específico dentro de um array de inteiros e retorna a posição em que ele se encontra.

## Funcionalidades:
- Procura valor em array.
- Retorna posição ou informa se não encontrado.

## Como usar:
Compile e execute:

```bash
gcc main.c -o search
./search
